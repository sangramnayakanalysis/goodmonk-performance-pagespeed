requests>=2.31.0
gspread>=6.1.0
google-auth>=2.29.0
python-dotenv>=1.0.1
# Customer Journey monitoring module (journey.py) only — every other
# script in this project has zero dependency on this package.
playwright>=1.45.0
